# Sample data

`sample_aapl_daily.json` is a saved Alpha Vantage `TIME_SERIES_DAILY` response used to make the repository reproducible without an API call.

To run the pipeline with current data, set `ALPHA_VANTAGE_API_KEY` in your local environment and omit the `--input-json` argument.
